// src/app/maps/elements/cables/layout.tsx
import type { ReactNode } from 'react';

export default function CablesLayout({ children }: { children: ReactNode }) {
  return <div className="h-full">{children}</div>;
}
