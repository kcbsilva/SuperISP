// src/app/maps/elements/foscs/layout.tsx
import type { ReactNode } from 'react';

export default function FoscsLayout({ children }: { children: ReactNode }) {
  return <div className="h-full">{children}</div>;
}
