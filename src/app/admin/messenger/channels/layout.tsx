// src/app/admin/messenger/channels/layout.tsx
import type { ReactNode } from 'react';

export default function MessengerChannelsLayout({ children }: { children: ReactNode }) {
  return <div className="h-full">{children}</div>;
}
