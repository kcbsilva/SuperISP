// src/app/maps/elements/polls/layout.tsx
import type { ReactNode } from 'react';

export default function HydroPollsLayout({ children }: { children: ReactNode }) {
  return <div className="h-full">{children}</div>;
}
