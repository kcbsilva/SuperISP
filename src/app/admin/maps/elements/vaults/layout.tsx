// src/app/maps/elements/vaults/layout.tsx
import type { ReactNode } from 'react';

export default function VaultsLayout({ children }: { children: ReactNode }) {
  return <div className="h-full">{children}</div>;
}
