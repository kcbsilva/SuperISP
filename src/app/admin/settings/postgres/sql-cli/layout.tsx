// src/app/admin/settings/postgres/sql-cli/layout.tsx
import type { ReactNode } from 'react';

export default function PostgresSqlCliLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
