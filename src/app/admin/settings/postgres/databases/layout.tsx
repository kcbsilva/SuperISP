// src/app/admin/settings/postgres/databases/layout.tsx
import type { ReactNode } from 'react';

export default function PostgresDatabasesLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
