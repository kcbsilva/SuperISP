// src/app/admin/settings/postgres/tables/layout.tsx
import type { ReactNode } from 'react';

export default function PostgresTablesLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
