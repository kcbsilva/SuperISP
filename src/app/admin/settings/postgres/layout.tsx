// src/app/admin/settings/postgres/layout.tsx
import type { ReactNode } from 'react';

export default function PostgresSettingsLayout({ children }: { children: ReactNode }) {
  return <div className="h-full">{children}</div>;
}
