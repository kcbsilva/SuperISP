// src/app/settings/network/cgnat/layout.tsx
import type { ReactNode } from 'react';

export default function CgnatLayout({ children }: { children: ReactNode }) {
  return <div className="h-full">{children}</div>;
}
