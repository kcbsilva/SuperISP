// src/app/admin/inventory/manufacturers/layout.tsx
import type { ReactNode } from 'react';

export default function ManufacturersLayout({ children }: { children: ReactNode }) {
  return <div className="h-full">{children}</div>;
}