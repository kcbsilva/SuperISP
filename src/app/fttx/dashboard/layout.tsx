// src/app/fttx/dashboard/layout.tsx
import type { ReactNode } from 'react';

export default function FTTxDashboardLayout({ children }: { children: ReactNode }) {
  return <div className="h-full">{children}</div>;
}
