// src/app/settings/system-monitor/layout.tsx
import type { ReactNode } from 'react';

export default function SystemMonitorLayout({ children }: { children: ReactNode }) {
  return <div className="h-full">{children}</div>;
}
